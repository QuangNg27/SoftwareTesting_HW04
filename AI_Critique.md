# AI Critique — Reflection on Human-AI Collaborative Automation Testing

Suốt quá trình tự động hóa kiểm thử, AI đã thể hiện khả năng vượt trội trong việc khởi tạo test script ban đầu, nhưng lại bộc lộ những xu hướng cố hữu về **Thiên vị Kịch bản Tích cực (Happy Path Bias)**, **xử lý bất đồng bộ thiếu ổn định (flaky timing)**, và **bỏ sót các khẳng định phủ định**. Cụ thể, ở tính năng FR-05 (`TC-FR05-006`), AI sử dụng selector chuỗi con lỏng lẻo (`text=...`) vô tình bắt trúng dòng tiêu đề tìm kiếm thay vì thẻ sản phẩm, dẫn đến lỗi False Positive nghiêm trọng trên ứng dụng SUT bị lỗi. Ở FR-14 (`TC-FR14-005`), AI thực thi khẳng định phủ định `not.toBeVisible()` ngay tức thì (0ms) sau khi gửi biểu mẫu trước khi backend API kịp phản hồi, che giấu lỗi vi phạm độ dài biên. Ngoài ra, trong FR-09, AI đã bỏ qua các bộ lắng nghe hộp thoại alert native (`window.alert`) cho luồng thanh toán chưa xác thực và liên tục gây lỗi xung đột strict-mode do không cô lập được các phần tử tiền tệ lặp lại trên cây DOM.

Nguyên nhân cốt lõi của những thất bại này bắt nguồn từ giới hạn bản chất của Mô hình Ngôn ngữ Lớn (LLM): AI sinh mã kiểm thử dựa trên các mẫu cú pháp tĩnh thay vì thấu hiểu động về vòng đời ứng dụng Single Page Application (SPA). Mô hình thiếu cảm nhận thực tế về độ trễ mạng, độ trễ gắn kết sự kiện (hydration lag) của React, sự khác biệt trong lập lịch microtask giữa các engine trình duyệt (như WebKit so với Chromium), và tính chất chặn luồng của các sự kiện cấp trình duyệt. Do đó, AI luôn có xu hướng tự nhiên là tạo ra các kịch bản vượt qua kiểm thử một cách hời hợt hơn là đào sâu phát hiện các lỗi tiềm ẩn của hệ thống.

Nguyên tắc quan trọng nhất rút ra từ sự hợp tác này là: **AI chỉ nên được xem như công cụ tăng tốc tạo khung mã nguồn, tuyệt đối không phải là một oracle kiểm thử tự động độc lập**. Kiểm thử viên con người bắt buộc phải duy trì sự phản biện nghiêm ngặt, thay thế các selector mơ hồ bằng các locator ngữ nghĩa theo vai trò trợ năng (role), đồng bộ hóa chặt chẽ phản hồi mạng trước khi khẳng định, và đảm bảo kịch bản kiểm thử phản ánh trung thực các lỗi thực tế của SUT theo đúng đặc tả.
